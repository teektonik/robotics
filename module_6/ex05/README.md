
1. Build the project

    ```bash
    colcon build
    ```

2. Source the workspace

    ```bash
    . ~/ws/install/setup.sh
    ```

3. Launch the simulation

    ```bash
    ros2 launch robot_depth robot_depth.launch.py
    ```
